# Generated by scripts/run_config_to_yaml.py from /iopsstor/scratch/cscs/thunter/shared_work/models/srdrwfy6/model_srdrwfy6.json
#
# Self-contained base config: pass it via --base-config so it replaces
# config/default_config.yml. Platform paths (data_path_*, model_path, mlflow, ...)
# are intentionally absent -- they come from the private config.
latent_noise_kl_weight: 0.0
latent_noise_gamma: 2.0
latent_noise_use_additive_noise: false
latent_noise_deterministic_latents: true
latent_noise_saturate_encodings: 5
embed_orientation: channels
embed_unembed_mode: block
embed_dropout_rate: 0.1
ae_local_dim_embed: 1024
ae_local_num_blocks: 4
ae_local_num_heads: 16
ae_local_dropout_rate: 0.1
ae_local_with_qk_lnorm: true
ae_local_num_queries: 1
ae_local_queries_per_cell: false
ae_adapter_num_heads: 16
ae_adapter_embed: 128
ae_adapter_with_qk_lnorm: true
ae_adapter_with_residual: true
ae_adapter_dropout_rate: 0.1
ae_global_dim_embed: 2048
ae_global_num_blocks: 5
ae_global_num_heads: 16
ae_global_dropout_rate: 0.1
ae_global_with_qk_lnorm: true
ae_global_att_dense_rate: 1.0
ae_global_block_factor: 64
ae_global_mlp_hidden_factor: 4
ae_global_trailing_layer_norm: true
ae_aggregation_num_blocks: 5
ae_aggregation_num_heads: 16
ae_aggregation_dropout_rate: 0.1
ae_aggregation_with_qk_lnorm: true
ae_aggregation_att_dense_rate: 1.0
ae_aggregation_block_factor: 64
ae_aggregation_mlp_hidden_factor: 4
decoder_type: PerceiverIOCoordConditioning
pred_adapter_kv: false
pred_self_attention: true
pred_dyadic_dims: false
pred_mlp_adaln: true
num_class_tokens: 0
num_register_tokens: 64
noise_pre_predictor_std: 0
fe_num_blocks: 16
fe_num_heads: 16
fe_dropout_rate: 0.1
fe_with_qk_lnorm: true
fe_layer_norm_after_blocks: [7]
fe_impute_latent_noise_std: 0
# Diffusion related parameters
frequency_embedding_dim: 256
embedding_dim: 512
noise_distribution: &noise_distribution "log_uniform"  # "log_uniform" or "log_normal"
sigma_min: &sigma_min 0.005
sigma_max: &sigma_max 235
sigma_data: 1.0
rho: 7
p_mean: 3.5  # only required if noise_distribution is log_normal
p_std: 1.2  # only required if noise_distribution is log_normal

forecast_att_dense_rate: 1.0
with_step_conditioning: true
healpix_level: 5
rope_2D: true
mlp_type: swiglu
use_xsa: true
with_mixed_precision: true
with_flash_attention: true
compile_model: false
with_fsdp: True # True #False
ddp_find_unused_parameters: false
attention_dtype: bf16
mixed_precision_dtype: bf16
mlp_norm_eps: 1.0e-05
norm_eps: 0.0001

#below for FE training only
freeze_modules: ".*latent_pre_norm.*|.*latent_heads.*|.*pred_heads.*|.*target_token_engines.*|.*embed_target_coords.*|.*encoder.*|.*StreamEmbedder_ERA5.*|.*embed_engine.*|.*embed_engine.*|.*ae_local_engine.*|.*ae_local_global_engine.*|.*ae_global_engine.*"
#below for DECODER training only
# freeze_modules: ".*latent_pre_norm.*|.*latent_heads.*|.*encoder.*|.*StreamEmbedder_ERA5.*|.*embed_engine.*|.*embed_engine.*|.*fe.*|.*ae_local_engine.*|.*ae_local_global_engine.*|.*ae_global_engine.*"
#below for FE and DECODER training
# freeze_modules: ".*latent_pre_norm.*|.*latent_heads.*|.*encoder.*|.*StreamEmbedder_ERA5.*|.*embed_engine.*|.*embed_engine.*|.*ae_local_engine.*|.*ae_local_global_engine.*|.*ae_global_engine.*"


norm_type: LayerNorm
qk_norm_type: RMSNorm
zarr_store: zip
streams_directory: ./config/streams/jepa_forecast_multi_data_all_years/

general: {
  istep: 0,
  rank: "???",
  world_size: "???",
  multiprocessing_method: spawn,
  run_id: "???",
  run_history: [],
  desc: '',
  }

train_logging: {
  terminal: 10,
  metrics: 20,
  checkpoint: 250,
  log_grad_norms: true,
  collapse_monitoring: {
    enabled: false,
    compute_frequency: 100,
    log_frequency: 100,
    metrics: {
      effective_rank: {
        enabled: true, tensor_source: both, sample_size: 2048, forecast_aggregation: all,
        },
      singular_values: {
        enabled: true, tensor_source: both, sample_size: 2048, forecast_aggregation: all,
        },
      dimension_variance: {
        enabled: true, tensor_source: both, forecast_aggregation: all,
        },
      prototype_entropy: { enabled: true, },
      ema_beta: { enabled: true, },
      },
    },
  }

data_loading: {
  num_workers: 6,
  rng_seed: "???",
  repeat_data_in_mini_epoch: false,
  persistent_workers: false,
  prefetch_factor: 1,
  memory_pinning: false,
  }


training_config: {

  training_mode: ["masking", "student_teacher"],
  teacher_time_offset: 0,

  deep_ssl: {
    enabled: false,
    tap_after: [local2global, "global:0", "global:1"],
    fusion_hidden_factor: 4,
    level_weights: [0.25, 0.25, 0.25, 0.25],
    },

  context_loss: { enabled: false, },

  num_mini_epochs: 128,
  samples_per_mini_epoch: 4096,
  shuffle: true,

  start_date: "1980-01-01T00:00",
  end_date: "2022-12-31T00:00",

  time_window_step: "06:00:00",
  time_window_len: "06:00:00",
  window_offset_prediction: 0,

  learning_rate_scheduling: {
    lr_start: 1.0e-06,
    lr_max: 1.0e-05,
    lr_final_decay: 1.0e-06,
    lr_final: 0.0,
    num_steps_warmup: 256,
    num_steps_cooldown: 512,
    policy_warmup: cosine,
    policy_decay: constant,
    policy_cooldown: linear,
    parallel_scaling_policy: sqrt,
    },

  optimizer: {
    grad_clip: 1.0,
    weight_decay: 0.1,
    adamw: {
      beta1: 0.975,
      beta2: 0.9875,
      eps: 2e-08,
      },
    log_grad_norms: false,
    },

  losses: {
    "student-teacher": {
      enabled: false,
      type: Disabled,
      weight: 1.0,
      loss_fcts: {
        JEPA: {
          weight: 1,
          loss_extra_args: { temporal: true, },
          out_dim: 2048,
          head: transformer,
          num_blocks: 6,
          num_heads: 16,
          with_qk_lnorm: true,
          intermediate_dim: 1024,
          dropout_rate: 0.1,
          target_source_correspondence: { '0': { '0': independent, }, },
          },
        },
      target_and_aux_calc: {
        EMATeacher: {
          ema_ramp_up_ratio: null,
          ema_halflife_in_thousands: 400000,
          model_param_overrides: {
            training_config: {
              losses: {
                "student-teacher": {
                  loss_fcts: { JEPA: { head: identity, }, },
                  },
                },
              },
            },
          },
        },
      },
    "forecast": {
      type: LossPhysical,
      enabled: False,
      loss_fcts: { "mse": { }, },
      },
    # "physical": {
    #   type: LossPhysical,
    #   weight: 0.0,
    #   loss_fcts: { "mse": { }, },
    #   target_and_aux_calc: "Physical",
    #   },
    "latent_diff": {
      type: LossLatentDiffusion,
      weight: 1.0,
      target_and_aux_calc: DiffusionLatentTargetEncoder,
      loss_fcts: { "mse": { }, },
      },
    },

  model_input: {
    "random_easy": {
      masking_strategy: forecast,
      num_samples: 0,
      num_steps_input: 1,
      masking_strategy_config: { },
      enabled: false,
      },
    "forecasting" : {
      masking_strategy: "forecast",
      masking_strategy_config: {
        diffusion_rn: True,
        # noise_distribution: *noise_distribution,
        # sigma_min: *sigma_min,
        # sigma_max: *sigma_max,
        },
      num_steps_input: 2,
      num_samples: 1,
      },
    },

  target_input: {
    "random_easy_target": {
      masking_strategy: forecast,
      num_samples: 1,
      masking_strategy_config: { },
      enabled: false,
      },
    "forecasting" : {
      masking_strategy: "forecast",
      masking_strategy_config: {
        diffusion_rn: True,
        noise_distribution: *noise_distribution,
        sigma_min: *sigma_min,
        sigma_max: *sigma_max,
        },
      num_steps_input: 1,
      num_samples: 1,
      },
    },

  forecast: {
    time_step: "06:00:00",
    num_steps: 1,
    offset: 0,
    policy: "fixed",
    },

  }

validation_config: {

  validation_noise_levels: [1.0, 2.0, 3.0, 4.0],

  samples_per_mini_epoch: 256,
  shuffle: false,
  start_date: "2023-10-01T00:00",
  end_date: "2023-12-31T00:00",
  validate_with_ema: {
    enabled: true,
    ema_ramp_up_ratio: 0.09,
    ema_halflife_in_thousands: 1e-3,
    },
  output: {
    num_samples: 0,
    normalized_samples: false,
    streams: null,
    },
  validate_before_training: False,
  }

wgtags: {
  org: null,
  issue: null,
  exp: null,
  grid: null,
  }

# test_config: {
#   samples_per_mini_epoch: 128,
#   shuffle: false,
#   start_date: "2023-06-01T00:00",
#   end_date: "2023-08-31T00:00",
#   output: {
#     num_samples: 128,
#     normalized_samples: false,
#     streams: null,
#     },
#   }
