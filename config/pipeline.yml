stages:
  - name: pretrain
    stage: train
    config_files:
      - config/config_forecasting.yml
    options:
      - training_config.mini_epochs=16
    chain_jobs: 2
    nodes: 8
    slurm_args:
      - "--time=23:59:00"
